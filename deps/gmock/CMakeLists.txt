set(GMOCK_INCLUDE_DIR ${PROJECT_BINARY_DIR}/include PARENT_SCOPE)
set(GMOCK_LIBRARIES ${PROJECT_BINARY_DIR}/lib/libgmock.a)
set(GMOCK_LIBRARIES ${GMOCK_LIBRARIES} PARENT_SCOPE)

set(GTEST_INCLUDE_DIR ${PROJECT_BINARY_DIR}/include PARENT_SCOPE)
set(GTEST_LIBRARIES ${PROJECT_BINARY_DIR}/lib/libgtest.a)
set(GTEST_LIBRARIES ${GTEST_LIBRARIES} PARENT_SCOPE)

add_custom_command(OUTPUT ${GMOCK_LIBRARIES} ${GTEST_LIBRARIES} DEPENDS veilhelpersBuild)
add_custom_target(gmock DEPENDS ${GMOCK_LIBRARIES})
add_custom_target(gtest DEPENDS ${GTEST_LIBRARIES})